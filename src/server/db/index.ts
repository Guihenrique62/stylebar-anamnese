/**
 * Camada de acesso a dados.
 *
 * Decisão pendente de implementação: Prisma + PostgreSQL (Neon ou Supabase).
 * Todo acesso ao banco passa por este módulo; páginas e route handlers não
 * importam o client diretamente, usam os serviços em src/server/*.
 */
export {};
